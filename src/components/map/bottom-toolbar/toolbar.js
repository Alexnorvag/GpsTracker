import React from 'react';
import {View, Text} from 'react-native';

const Toolbar = ({styles}) => {
  return (
    <View style={styles}>
      <Text>Toolbar</Text>
    </View>
  );
};

export default Toolbar;
